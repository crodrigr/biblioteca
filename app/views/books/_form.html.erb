<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  


<div class="form-group">
    <%= form.label :isbn, "Isbn", class: "col-md-4 control-label" %>
    <div class="col-md-8">
    <%= form.text_field :isbn, class: "form-control" %>
    </div>
 </div>

 <div class="form-group">
    <%= form.label :titulo, class: "col-md-4 control-label" %>
    <div class="col-md-8">
    <%= form.text_field :titulo,class: "form-control"  %>
   </div>
  </div> 

  <div class="form-group">
    <%= form.label :paginas, class: "col-md-4 control-label"  %>
    <div class="col-md-8">
    <%= form.number_field :paginas,class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= form.label :editorial, class: "col-md-4 control-label" %>
    <div class="col-md-8">
    <%= form.text_field :editorial,class: "form-control" %>
  </div>
  </div>  
  <%= form.submit "Guardar", class: "btn btn-default btn-primary" %>
  <%= link_to "Cancel",books_path , {:class=>"btn btn-default btn-primary"} %>
<% end %>
